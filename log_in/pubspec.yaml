name: log_in

description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
  english_words: ^4.0.0
  provider: ^6.0.0
  google_fonts: ^6.2.1
  date_time_picker: ^2.1.0
  shared_preferences: ^2.2.3
  fluttertoast: ^8.2.6
  #camera
  camera: ^0.11.0+1
  path_provider: ^2.1.3
  path: ^1.9.0
  image_picker: ^1.1.2
  #for date and time
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/phone-male.png
